---
import SiteLayout from './SiteLayout.astro';
---

<SiteLayout>
  <div class="shell">
    <article class="home prose">
      <slot />
    </article>
  </div>
</SiteLayout>

<style>
  .home {
    max-width: 48rem;
    padding-top: var(--space-7);
  }

  .home :global(p:not(:has(img))) {
    max-width: 60ch;
    color: var(--ink-soft);
    font-size: var(--step-1);
    line-height: 1.6;
  }

  .home :global(h1 + p) {
    margin-top: var(--space-3);
  }
</style>
